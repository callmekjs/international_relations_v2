  × No solution found when resolving dependencies:
  ╰─▶ Because the requested Python version (>=3.10) does not satisfy
      Python>=3.12 and numpy==2.5.3 depends on Python>=3.12, we can conclude
      that numpy==2.5.3 cannot be used.
      And because you require numpy==2.5.3, we can conclude that your
      requirements are unsatisfiable.

      hint: The `--python-version` value (>=3.10) includes Python versions
      that are not supported by your dependencies (e.g., numpy==2.5.3 only
      supports >=3.12). Consider using a higher `--python-version` value.
